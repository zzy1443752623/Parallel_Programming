这段代码是基于NVIDIA公司旗下的GPU并行计算框架CUDA，利用GPU中的thread对二维矩阵（1024*1024）进行转置操作。

这里需要注意的是，在并行编程的领域里，我们都寄希望于GPU访问内存（读或写），数据的存放地址是连续的。
该原因在于，GPU每次访问内存时，总是访问较多连续地址的数据。而如果我们可以把需要处理的数据存放在连续地址的内存中，则GPU可以以较少的访问内存成本，完成数据的搬移工作。